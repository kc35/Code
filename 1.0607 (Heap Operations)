#include <stdio.h>
int size = 0;
void swap(int *a1, int *b1)
{
  int temp = *b1;
  *b1 = *a1;
  *a1 = temp;
}

// algorithm to check whether the values of root, left child as well as right child are larger compared to each other and swap them.
void heapI(int array[], int size, int f)
{
  if (size == 1)
  {
    printf("Single element in the heap");
  }
  else
  {
    int largest = f;
    int l = 2 * f + 1;
    int r = 2 * f + 2;
    if (l < size && array1[l] > array1[largest])
      largest = l;
    if (r < size && array1[r] > array1[largest])
      largest = r;
    if (largest != f)
    {
      swap(&array1[f], &array1[largest]);
      heapI(array1, size, largest);
    }
  }
}

//insert new numbers
void insert(int array1[], int newNum)
{
  if (size == 0)
  {
    array1[0] = newNum;
    size += 1;
  }
  else
  {
    array1[size] = newNum;
    size += 1;
    for (int f = size / 2 - 1; i >= 0; i--)
    {
      heapI(array1, size, f);
    }
  }
}
void extract_min(int array1[], int num)
{
  int f;
  for (f = 0; f < size; f++)
  {
    if (num == array1[f])
      break;
  }

  swap(&array1[f], &array1[size - 1]);
  size -= 1;
  for (int i = size / 2 - 1; i >= 0; i--)
  {
    heapI(array1, size, f);
  }
}
void printArray1(int array1[], int size)
{
  for (int f = 0; f < size; ++f)
    printf("%d ", array1[f]);
  printf("\n");
}
int main()
{
  int array1[10];

  insert(array1, 3);
  insert(array1, 4);
  insert(array1, 9);
  insert(array1, 5);
  insert(array1, 2);

  printf("Max-Heap array: ");
  printArray(array1, size);

  delRoot(array1, 4);

  printf("After deleting an element: ");

  printArray(array1, size);
}
