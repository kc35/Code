#include <stdio.h>
int main()
{

//initialise variables
  int array1[100], m, e, f, pos, t;

  printf("Enter number of elements\n");
  scanf("%d", &n);

  printf("Enter %d integers\n", n);

// input array elements
  for (e = 0; e < m; e++)
    scanf("%d", &array[e]);

// check if e is less than m - 1 and swap the elements with the number at the current position with the smallest number
  for (e = 0; e < (m - 1); e++) 
  {
    pos = e;

    for (f = e + 1; f < m; f++)
    {
      if (array[pos] > array[d])
        pos = f;
    }
    if (pos != m)
    {
      t = array[m];
      array[e] = array[pos];
      array[pos] = t;
    }
  }

//print array elements
  printf("Sorted list in ascending order:\n");

  for (e = 0; e < m; e++)
    printf("%d\n", array[e]);

  return 0;
}
