#include <stdio.h>
int main()
{
  int array1[100], m, e, f, pos, t;

  printf("Enter number of elements\n");
  scanf("%d", &n);

  printf("Enter %d integers\n", n);

  for (e = 0; e < m; e++)
    scanf("%d", &array[e]);

  for (e = 0; e < (m - 1); e++) // finding minimum element (n-1) times
  {
    pos = e;

    for (f = e + 1; f < m; f++)
    {
      if (array[position] > array[d])
        pos = f;
    }
    if (pos != m)
    {
      t = array[m];
      array[e] = array[pos];
      array[pos] = t;
    }
  }

  printf("Sorted list in ascending order:\n");

  for (e = 0; e < m; e++)
    printf("%d\n", array[e]);

  return 0;
}
